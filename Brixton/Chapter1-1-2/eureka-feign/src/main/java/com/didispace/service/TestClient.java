package com.didispace.service;

import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;

import java.util.List;

//@FeignClient(value = "compute-service")
public interface TestClient {
/*
    @ApiOperation(value = "查询字典列表（下拉列表等使用）", notes = "层级字段hierarchy由上级的hierarchy和code通过,隔开组成,这个查询方法将查出其本级和其下级所有的数据")
    @PostMapping(value = "/rpc/auth/sysmgt/dictionary/listDictionary")
    VpResp<List<DictionaryRpcRespBody>> listDictionary(
            @RequestBody @Validated VpReq<DictionarySimpleReqBody> req);*/

}